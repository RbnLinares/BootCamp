/** 
* findIntersection function return an array that
* contain the numbers exist in both strings from the array.
* ["1,2,5,6,7", "2,5,7,8,15"] => [2,5,7]
*/

// function findIntersection(arr) {
//     const firstArray = arr[0].split(',').map(Number);
//     const secondArray = arr[1].split(',').map(Number);

//     const intersection = firstArray.filter(value => secondArray.includes(value));

//     return intersection;
// }

// const result = findIntersection(["1,2,5,6,7", "2,5,7,8,15"]);
// console.log(result); // Output: [2, 5, 7]


